# ハンターハンター クイズゲーム 設計書

最終更新: 2026-09-13
状態: ローカル動作確認済み。一ツ星〜三ツ星 各 20 問、おまめ専用 20 問を実装 (草案、原作確認は一部未了)。GitHub 公開待ち。

## 1. 要件(確定)

- GitHub Pages で公開する静的サイト(サーバー側処理なし)
- 難易度: 一ツ星 / 二ツ星 / 三ツ星 / おまめ専用 の4段階。すべてトップ画面に表示し、誰でも選べる。定義と作成ルールは `docs/question/README.md`
- 問題は `docs/question/` に 1 問 1 ファイルの Markdown で管理する。Claude がそれを読んで TypeScript データと UI に実装する(自動パーサなし)
- 記述式(自由入力)は採用しない。空欄は候補選択に置き換える
- 1プレイの流れ: 難易度のみ選択 → 形式混在の10問をランダム抽出 → 各問で正誤+解説 → 結果画面
- 画像は使わない(テキストのみ)
- ネタバレ配慮(編タグフィルタ)は不要
- 他人とのランキングは不要。自分の難易度別最高記録のみ localStorage に保存

## 2. 決定事項の経緯

### 2026-09-11
| # | 質問 | 決定 |
|---|---|---|
| 1 | シート反映のタイミング | 手動同期で十分 |
| 2 | おまめ専用の対象 | 誰でも選べてよい |
| 3 | 1プレイの流れ | 難易度のみ選択、形式は混在 |
| 4 | 並べ替えの要素数 | 3〜4個 |
| 5 | 画像 | テキストのみ |
| 6 | ネタバレ配慮 | 不要 |
| 7 | ランキング | 不要(自分の記録のみ) |

### 2026-09-13 難易度 1〜3 の定義
- 知識の深さで分ける。一ツ星: 主要キャラ・出来事・念の基本 (マルバツ・4択)。二ツ星: 各編の細部・サブキャラ・順序 (+ 並べ替え・仕分け)。三ツ星: 数値・ルールの細部 (全形式)
- 各 20 問。Claude が草案を作り、確信度 ★★☆ の問題は作成者が原作で確認する
- 問題作成のルールを `docs/question/README.md` に明文化

### 2026-09-13 方針変更
- 仕分け・組み合わせ問題をドラッグ&ドロップ (タップ操作も併用可) に変更。組み合わせは使った候補を再利用不可にし、誤り探しは「誤っている / 正しい」の箱に全カードを仕分ける形にして誤読を防ぐ
- スプレッドシート管理をやめ、Markdown ファイル(`docs/question/`)から Claude が実装する方式に変更。同期スクリプトとシート由来の問題は削除
- 「HxH 応用クイズ 10 問」をおまめ専用として実装
- 記述式の問題は候補選択に変更(ダミー候補を混ぜる)
- 並べ替えの要素数上限を 6 個に緩和(5 項目の問題があるため)

## 3. アーキテクチャ

### 3.1 問題の管理フロー
1. `docs/question/q<番号>_<題名>.md` に問題を書く(テンプレートは `docs/question/README.md`)
2. Claude に「docs/question を実装に反映して」と依頼する
3. Claude が `src/data/<難易度など>.ts` に型付きデータとして追加し、新形式があれば `src/quiz/` に UI を追加する
4. `npm test` でデータ整合性テスト(`tests/questions.test.ts`)が通ることを確認する

### 3.2 出題形式と型

| 形式 | 型 | 回答 UI | 判定 |
|---|---|---|---|
| マルバツ | `tf` | ◯ / × ボタン | 一致 |
| 4択 | `choice` | 選択肢をシャッフルして表示 (`shuffle: false` で固定)。`text` に本文を付けられ、`[語]` は破線下線で強調 (誤り箇所の選択など) | 一致 |
| 並べ替え | `order` | 候補をタップして順に積む。戻す / 決定 | 順列の完全一致 |
| 仕分け | `multi` | 各カードを「対象」「対象外」の箱へドラッグ (またはカードのボタンで移動)。箱の名前は `bins` で指定 (例: 誤っている / 正しい) | `pick` 未指定: 全カードを仕分け、対象の箱の集合が完全一致。`pick` 指定: 対象の箱にちょうど pick 枚で全て正解集合に含まれる (残りは未分類でよい) |
| 仕分け＋並べ替え | `pickorder` | 仕分けと同じ 2 箱構成。対象の箱は番号付きで、↑↓ ボタンかカード上へのドロップで順番を変える | 対象の箱の並びが `ordered` と完全一致し、残りがすべて対象外の箱にある |
| 穴埋め・組み合わせ | `fill` | `pool` あり: 共通候補を解答欄へドラッグ、または解答欄をタップして候補をタップ。使った候補は候補欄から消える (単一使用)。`pool` なし: 解答欄をタップしてその行の候補から選ぶ | 全行一致。行ごとの正誤と正解も表示 |

型定義は `src/types.ts`。`kind` で「誤り探し」「マッチング」など形式名の補足を表示できる。

ドラッグ&ドロップは `src/quiz/dnd.ts` の共通実装。マウスは 6px 動かすと開始、タッチは 250ms 長押しで開始 (長押し前に動けばスクロール)。
「誤っているものを選べ」を「正しいものを選べ」と誤読しないよう、箱の名前を明示して全カードを仕分けさせる。

### 3.3 データ検証(`tests/questions.test.ts`)
- id 重複なし、難易度は 1〜4、問題文が空でない
- choice: 選択肢 2〜6 個で重複なし、answer が範囲内
- order: 要素 3〜6 個、重複なし
- multi: 候補 3 つ以上で重複なし、answers が範囲内で重複なし。`pick` 指定時はダミー候補が 1 つ以上ある。`bins` の 2 つは異なる名前
- pickorder: 候補 3 つ以上で重複なし、ordered が範囲内で重複なく 2 つ以上かつ候補数未満、`bins` の 2 つは異なる名前
- fill: `pool` あり → 候補に重複なし、各行の answer が pool に含まれ行ごとに異なる (単一使用のため)。`pool` なし → 各行に候補 2 つ以上、重複なし、answer を含む

### 3.4 技術スタック: Vite + TypeScript + vitest
- GitHub Actions で `dist` を Pages にデプロイ(`.github/workflows/deploy.yml`)

ディレクトリ構成:
```
h_h_quiz/
  index.html
  docs/question/        # 問題の原稿 (1 問 1 ファイル)
  src/
    main.ts             # 画面遷移 (タイトル → 出題 → 結果)
    types.ts            # 問題の型
    data.ts             # 抽出・シャッフル
    data/index.ts       # 全問題の集約と DATA_VERSION
    data/star1.ts〜star3.ts  # 一ツ星〜三ツ星の問題データ
    data/omame.ts       # おまめ専用の問題データ
    quiz/               # 形式ごとの UI (tf, choice, order, multi, fill, pickorder) と dnd.ts
    storage.ts          # localStorage
    style.css
  tests/
  .github/workflows/deploy.yml
```

### 3.5 UI / ゲーム設計
- 画面遷移: トップ(難易度選択・最高記録表示) → 出題(10問) → 結果
- 出題: 選んだ難易度の全問題から10問をランダム抽出(形式混在)。問題数が10未満ならあるだけ出す
- 各問回答後に正誤と解説を表示し、「次へ」で進む
- 結果画面: 正解数、各問の正誤一覧、最高記録更新の表示、「もう一度」「トップへ」
- 記録: `localStorage` に難易度別の最高正解数のみ保存
- モバイルファースト

### 3.6 権利・免責
- 公式画像・長い本文引用は使わない(テキストのみ)
- README に非公式・非営利のファンプロジェクトである旨と権利者向け連絡先を記載
- 問題の内容(正誤)は AI 生成を含むため、原作での確認は作成者の責任で行う

## 4. 参考にする既存リポジトリ

- `../pokemon-speed-quiz`: Vite + TS + vitest、Pages デプロイ
- `../online-shogi`: ビルドなし素のJS、CSP 設定、画面切り替えの構成

## 5. 実装ステップ

1. [完了 2026-09-12] Vite 雛形(TypeScript)、vitest 導入
2. [完了 2026-09-12] 画面実装(トップ → 出題 → 結果)、抽出ロジックとそのテスト
3. [完了 2026-09-13] 複数選択・穴埋め形式の UI、おまめ専用 10 問 (+ 2026-09-13 に 10 問追加して 20 問)、データ整合性テスト
4. [草案完了 2026-09-13] 一ツ星〜三ツ星の問題を各 20 問追加して実装 (`src/data/star1〜3.ts`)。★★☆ の問題の原作確認は未了
5. [未着手] `git init`、GitHub リポジトリ作成、Pages デプロイ設定(workflow は作成済み)
